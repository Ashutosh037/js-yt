const marvel_h = ["thor"]
const dc_h = ["aqua","super"]

//spread to merge two arrays into one    you can also use concat

const myna = [...marvel_h, ...dc_h]
console.log(myna)

//flat can be used to convert every array that is inside an array into one array