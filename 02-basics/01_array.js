const myarr = [1,2,3,4,5]
// console.log([0])


// myarr.push(6)  // it adds the element in the last
// console.log(myarr)

// myarr.pop()  //it removes the last element 

// myarr.unshift(0) //it adds the element in the starting

//console.log(myarr.includes(9))  //it checks the element is included or not and return the boolean value
//console.log(myarr)

const newarr = myarr.join()  //it joins the element and converted it into a string
console.log(newarr)
console.log(typeof newarr)

//slice
//splice